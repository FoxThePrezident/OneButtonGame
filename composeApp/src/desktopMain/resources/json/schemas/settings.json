{
  "title": "Settings for player and game",
  "description": "Storing user specified settings.",
  "type": "object",
  "properties": {
    "player": {
      "type": "object",
      "properties": {
        "controlDelay": {
          "type": "integer",
          "minimum": 1
        },
        "radius": {
          "type": "integer",
          "minimum": 1
        }
      },
      "required": [
        "controlDelay",
        "radius"
      ]
    }
  },
  "required": [
    "player"
  ]
}